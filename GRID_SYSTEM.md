# Grid System 구현 완료

## 🎯 목적
픽셀 단위 이동에서 그리드 기반 좌표 시스템으로 전환하여 향후 스프라이트 렌더링 시 성능 최적화

## 📋 구현 내용

### 1. 상수 추가 ([src/constants.py](src/constants.py))

```python
# 그리드 설정
GRID_WIDTH = 50  # 각 그리드 셀의 픽셀 너비
GRID_HEIGHT = 50  # 각 그리드 셀의 픽셀 높이

# 그리드 개수 (자동 계산)
GRID_COLS = 28  # 1400 / 50
GRID_ROWS = 28  # 1400 / 50

# 플레이어 설정 (그리드 단위)
PLAYER_GRID_SIZE = 1  # 그리드 셀 단위
PLAYER_SPEED_GRID = 1  # 한 번 이동 시 그리드 셀 개수

# 적 설정 (그리드 단위)
ENEMY_GRID_SIZE = 1
ENEMY_SPEED_GRID = 0.5  # 부드러운 이동을 위해 소수 허용
```

### 2. Player 클래스 업데이트

**주요 변경사항:**
- `x, y` → `grid_x, grid_y` (그리드 좌표)
- 이동 시 쿨다운 추가 (8 프레임마다 1칸 이동)
- `get_pixel_pos()` 메서드 추가: 그리드 → 픽셀 변환
- 렌더링 시 픽셀 좌표로 자동 변환

**장점:**
- 격자 기반 정확한 이동
- 키 입력 쿨다운으로 자연스러운 조작감
- 나중에 타일맵과 쉽게 통합 가능

### 3. Enemy 클래스 업데이트

**주요 변경사항:**
- `x, y` → `grid_x, grid_y` (float 지원)
- 그리드 좌표에서 추적 로직 수행
- 부드러운 이동을 위해 `ENEMY_SPEED_GRID = 0.5` 사용
- 화면 경계 제한 추가

**장점:**
- 적은 부드럽게 이동 (float 좌표)
- 플레이어는 격자 단위로 이동
- 게임플레이 밸런스 향상

### 4. Game 클래스 업데이트

**주요 변경사항:**
- 플레이어 시작 위치: 그리드 중앙 (14, 14)
- 적 spawn 위치: 그리드 경계
- 충돌 판정은 픽셀 단위로 변환되어 정확하게 동작

## 🎮 게임플레이 변화

### 이전 (픽셀 기반)
- 매우 부드러운 이동
- 픽셀 단위 정밀 제어
- 스프라이트 렌더링 시 부하 우려

### 현재 (그리드 기반)
- **플레이어**: 격자 단위 이동 (쿨다운 적용)
- **적**: 부드러운 추적 이동 (float 좌표)
- 성능 최적화 준비 완료
- 타일맵 확장 가능

## ✅ 테스트 결과

```
38개 테스트 모두 통과 ✅
- Player 테스트: 13개 ✅
- Enemy 테스트: 10개 ✅
- Game 테스트: 15개 ✅
```

## 🔧 설정 변경 방법

그리드 크기를 변경하려면 [src/constants.py](src/constants.py)에서:

```python
# 타일 크기 변경 예시
GRID_WIDTH = 32   # 더 작은 타일
GRID_HEIGHT = 32  # 43 x 43 그리드

# 또는
GRID_WIDTH = 100  # 더 큰 타일
GRID_HEIGHT = 100 # 14 x 14 그리드
```

## 📊 성능 이점

1. **메모리 효율**: 그리드 좌표는 정수/작은 float
2. **렌더링 최적화**: 스프라이트를 그리드 단위로 캐싱 가능
3. **충돌 판정**: 그리드 기반 공간 분할로 최적화 가능
4. **확장성**: 타일맵, 장애물, 아이템 배치 용이

## 🎯 향후 확장 가능성

- **타일 기반 맵**: 각 그리드 셀에 타일 정보 저장
- **스프라이트 애니메이션**: 그리드 단위로 스프라이트 전환
- **장애물 시스템**: 그리드 좌표로 충돌 체크
- **미니맵**: 그리드 기반 간단한 표시

## 💡 구현 하이라이트

### 좌표 변환
```python
def get_pixel_pos(self):
    """그리드 좌표를 픽셀 좌표로 변환"""
    pixel_x = self.grid_x * GRID_WIDTH
    pixel_y = self.grid_y * GRID_HEIGHT
    pixel_width = self.grid_size * GRID_WIDTH
    pixel_height = self.grid_size * GRID_HEIGHT
    return (pixel_x, pixel_y, pixel_width, pixel_height)
```

### 플레이어 이동 쿨다운
```python
def move(self, keys):
    # 쿨다운 감소
    if self.move_cooldown > 0:
        self.move_cooldown -= 1
        return
    
    # 이동 로직...
    if moved:
        self.move_cooldown = self.move_delay  # 8 프레임
```

### 적의 부드러운 이동
```python
# float 좌표 지원
self.grid_x = float(grid_x)
self.grid_y = float(grid_y)
self.speed = 0.5  # 반 그리드씩 이동

# 매 프레임 부드럽게 이동
self.grid_x += direction_x * self.speed
self.grid_y += direction_y * self.speed
```

## 🎉 결과

- ✅ 픽셀 기반에서 그리드 기반으로 성공적 전환
- ✅ 모든 테스트 통과
- ✅ 게임플레이 유지
- ✅ 성능 최적화 준비 완료
- ✅ 향후 확장성 확보

그리드 크기는 `constants.py`에서 언제든 조정 가능하며, 자동으로 그리드 개수가 재계산됩니다!
